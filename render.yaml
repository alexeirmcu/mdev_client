services:
  - type: web
    name: stp-client
    env: static
    buildCommand: cd client && npm install && npm run build
    staticPublishPath: client/dist
    envVars:
      - key: VITE_API_URL
        value: https://smart-trip-planner-api.onrender.com/api
